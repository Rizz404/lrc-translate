# Leave unset to use Vite's dev proxy (see vite.config.ts), which forwards /api
# to the backend on localhost:8080 — this way only the Vite port needs tunneling.
# Set this only if you need to point at a backend on a different host/port.
# VITE_API_BASE_URL=http://localhost:8080/api
